#0202
gc()

#0206-COMMENT ALL SAVE(, WRITE.CSV(, REMOVE ALL LINES CONTAINING READ.XLS

#Set base working directory
baseWD<-"D:\\R_SCRIPTS\\0508"
classPath<-"D:\\Softwares\\RedshiftJDBC41-1.1.10.1010.jar"


#Set up Current time for database entry and log file names
currentTime<-format(Sys.time(), "%Y-%m-%d %H:%M:%S")
currentTime_fName<-gsub("-","_",currentTime)
currentTime_fName<-gsub(" ","_",currentTime_fName)
currentTime_fName<-gsub(":","_",currentTime_fName)

#Enable Logging
LogFile<-paste(baseWD,"\\Log\\",currentTime_fName,"_Spanish_Combine_Text_Mining_Scoring_v16.0_Log.txt",sep="")
zz_Log <- file(LogFile, open="wt")
sink(zz_Log, type = "message")

ListFile<-paste(baseWD,"\\Log\\",currentTime_fName,"_Spanish_Combine_Text_Mining_Scoring_v16.0_List.txt",sep="")
zz_List <- file(ListFile, open="wt")
sink(zz_List, type = "output")


#Set libraries
library("RSQLite")
library(sqldf)
library(plyr)
library(stringr)
library("RPostgreSQL")
library("RJDBC")
library(tm)   
library(SnowballC)
library(dplyr)
library(stringr)
library(stringdist)
library(zoo)
library(xlsx)
library(qdap)
library(parallel)
library(snow)
#library(Rmpi)
library(stringi)
library(foreach)
library(doSNOW)
library(bigmemory)
library(biganalytics)
library(Matrix)
library(koRpus)
library(hunspell)


#Set Memory Limit
memory.limit()
memory.limit(size=16000000000000)
memory.limit()

#Set up Database Connection
drv_custom1 <- JDBC(driverClass = "com.amazon.redshift.jdbc41.Driver", classPath=classPath)
 
con <- dbConnect(drv_custom1,
                                                url="jdbc:redshift://dapcnvyranalytics.czsqals6k9uo.us-east-1.redshift.amazonaws.com:5439/conveyoranalytics",
                                                dbname = "conveyoranalytics",
                                                schemaname="common_tables",
                                                user = "dapappadmin",
                                                port = 5439,
                                                host = "dapcnvyranalytics.czsqals6k9uo.us-east-1.redshift.amazonaws.com",
                                                password = "Adm1n@aw$")
 
 


#Garbage Collection
gc()

#######################################################################
####0206-Create Error Log Entry and read input data
#######################################################################

#Change the schema
#Change the schema
dbGetQuery(con, "show search_path;")
dbSendUpdate(con, "set search_path to common_tables,public;")
dbGetQuery(con, "show search_path;")


#Create default Error log row
code_name<-paste(baseWD,"\\Code\\Spanish_Combine_Text_Mining_Scoring_v16.0.R",sep="")
code_description<-"Spanish Combine Text"
run_start_datetime<-currentTime
run_end_datetime<-NA
run_status<-"RUNNING or FAILURE"
run_status_message<-paste("CHECK LOG : ",LogFile, "AND LIST : ",ListFile,SEP="")
errorLogEntry<-data.frame(cbind(code_name,code_description,run_start_datetime,run_status,run_status_message,run_end_datetime))

#Write default Error log row to DB
dbWriteTable(con, "t_error_log", 
             value = errorLogEntry, overwrite=FALSE,append = TRUE, row.names = FALSE)
dbCommit(con)


#######################################################################
####0206-Disconnect from database
#######################################################################

#Disconnect from database
dbDisconnect(con)

#Disable DB libraries because these interfere with normal SQLDF calls
detach(package:RPostgreSQL, unload = TRUE)
detach(package:RJDBC, unload = TRUE)


#######################################################################
#### THIS CODE COMBINES ALL THE 4 OUTPUTS
#######################################################################

#Read all 4 output files generated by previous codes
getwd()
setwd(paste(baseWD,"\\Interim",sep=""))

Spanish_Short<-get(load("outputFinal7_Spanish_Short.rda"))
head(Spanish_Short)

Spanish_Long<-get(load("outputFinal7_Spanish_Long.rda"))
head(Spanish_Long)

nrow(Spanish_Short)
nrow(Spanish_Long)

gc()

#######################################################################################
### SPANISH
#######################################################################################

##Combine Spanish
Spanish_All<-rbind(Spanish_Short,Spanish_Long)
nrow(Spanish_All)
nrow(Spanish_All)==nrow(Spanish_Short)+nrow(Spanish_Long)

head(Spanish_All)

##Sort
Spanish_All2<-Spanish_All[order(Spanish_All$notification_number,Spanish_All$N_Tokens,Spanish_All$NLP_Cluster),]
head(Spanish_All2,100)
names(Spanish_All2)

####Pick only unique ones

#Create Vectors to compare
Noti_vector<-as.character(str_trim(Spanish_All2$notification_number))
length(Noti_vector)==nrow(Spanish_All2)

Cluster_vector<-toupper(as.character(str_trim(Spanish_All2$NLP_Cluster)))
length(Cluster_vector)==nrow(Spanish_All2)

Textlength_vector<-toupper(as.character(str_trim(Spanish_All2$Textlength)))
length(Textlength_vector)==nrow(Spanish_All2)

first<-numeric(nrow(Spanish_All2))

first[1]=1
for(i in 2:nrow(Spanish_All2)){
	if(paste(Noti_vector[i],Cluster_vector[i],sep="-")!=paste(Noti_vector[i-1],Cluster_vector[i-1],sep="-")){
		first[i]=1
	} else if (paste(Noti_vector[i],Cluster_vector[i],sep="-")==paste(Noti_vector[i-1],Cluster_vector[i-1],sep="-")){
		first[i]=0
		Textlength_vector[i]<-"BOTH"
		Textlength_vector[i-1]<-"BOTH"
	}
}

sum(first)
nrow(Spanish_All2)

nrow(Spanish_All2)-sum(first)
table(Textlength_vector)
sum(Textlength_vector=="BOTH")==2*(nrow(Spanish_All2)-sum(first))

head(Spanish_All2[(Textlength_vector=="BOTH"),"NLP_Cluster"],100)

#Bring in new text length column
Spanish_All3<-cbind(Spanish_All2,Textlength_vector)
head(Spanish_All3,20)
Spanish_All3<-Spanish_All3[,!(names(Spanish_All3)%in% "Textlength")]
names(Spanish_All3)
colnames(Spanish_All3)<-c(names(Spanish_All3)[!(names(Spanish_All3) %in% "Textlength_vector")],"Textlength")
names(Spanish_All3)

table(Spanish_All3$Textlength)
table(Textlength_vector)

#Keep only first record
Spanish_All4<-Spanish_All3[first==1,]
nrow(Spanish_All4)
table(Spanish_All4$Textlength)

nrow(Spanish_All4)==sum(first)
sum(Spanish_All4$Textlength=="BOTH")==(nrow(Spanish_All2)-sum(first))

##Find common Noti between long and short text
Short_Text_Noti_Spanish_Vector<-as.character(Spanish_All4[(Spanish_All4$Textlength %in% c("SHORT","BOTH")),"notification_number"])
Long_Text_Noti_Spanish_Vector<-as.character(Spanish_All4[(Spanish_All4$Textlength %in% c("LONG","BOTH")),"notification_number"])


Short_Text_Noti_Spanish_Vector<-unique(Short_Text_Noti_Spanish_Vector)
Long_Text_Noti_Spanish_Vector<-unique(Long_Text_Noti_Spanish_Vector)

length(Short_Text_Noti_Spanish_Vector)
length(Long_Text_Noti_Spanish_Vector)

Common_Text_Noti_Spanish_Vector<-Short_Text_Noti_Spanish_Vector[Short_Text_Noti_Spanish_Vector %in% Long_Text_Noti_Spanish_Vector]
length(Common_Text_Noti_Spanish_Vector)

Spanish_All4$isCommon_Noti_Short_Long<-ifelse(Spanish_All4$notification_number %in% Common_Text_Noti_Spanish_Vector,1,0)
table(Spanish_All4$isCommon_Noti_Short_Long)
head(Spanish_All4)
names(Spanish_All4)
nrow(Spanish_All4)

table(Spanish_All4$Textlength)
table(Spanish_All4$isCommon_Noti_Short_Long,Spanish_All4$Textlength)

## Keep only tokens <=3
Spanish_All5<-Spanish_All4[as.numeric(as.character(Spanish_All4$N_Tokens))<=3,]
nrow(Spanish_All5)
table(Spanish_All5$N_Tokens)

#0508- Bring Current timestamp 
head(Spanish_All5)
Spanish_All5$creation_date<-currentTime
head(Spanish_All5)

#0508- Arrange columns
names(Spanish_All5)
Spanish_All5<-Spanish_All5[,c("notification_number","notification_description","NLP_Cluster","N_Tokens","NLP_Name_Source","Textlength","Language","isCommon_Noti_Short_Long","notification_datetime","creation_date")]
head(Spanish_All5)
setwd(paste(baseWD,"\\Interim",sep=""))
save(Spanish_All5,file="Spanish_All5.rda")


#######################################################################
####0206-Database Write
#######################################################################

#Set libraries
library("RPostgreSQL")
library("RJDBC")

#Set up Database Connection
drv_custom1 <- JDBC(driverClass = "com.amazon.redshift.jdbc41.Driver", classPath=classPath)
 
con <- dbConnect(drv_custom1,
                                                url="jdbc:redshift://dapcnvyranalytics.czsqals6k9uo.us-east-1.redshift.amazonaws.com:5439/conveyoranalytics",
                                                dbname = "conveyoranalytics",
                                                schemaname="common_tables",
                                                user = "dapappadmin",
                                                port = 5439,
                                                host = "dapcnvyranalytics.czsqals6k9uo.us-east-1.redshift.amazonaws.com",
                                                password = "Adm1n@aw$")
 
 


#################### UPDATE FINAL OUTPUT ######################################

#Change the schema
dbGetQuery(con, "show search_path;")
dbSendUpdate(con, "set search_path to naturallanguageprocessing,public;")
dbGetQuery(con, "show search_path;")

#Spanish
setwd(paste(baseWD,"\\Interim",sep=""))
Spanish_All5<-get(load("Spanish_All5.rda"))

head(Spanish_All5)
Spanish_All5$notification_description<-NA
head(Spanish_All5)

#0508- Arrange columns
Spanish_All5<-Spanish_All5[,c("notification_number","notification_description","NLP_Cluster","N_Tokens","NLP_Name_Source","Textlength","Language","isCommon_Noti_Short_Long","notification_datetime","creation_date")]

setwd(paste(baseWD,"\\Output",sep=""))
write.table(Spanish_All5, "Spanish_All5.txt",
            na = "",
            row.names = FALSE,
            col.names = TRUE,
            sep = "\t")




#################### UPDATE ERROR LOG ######################################

# Change run status to SUCCESS if writing of results and metadata is successful

#Change the schema
dbGetQuery(con, "show search_path;")
dbSendUpdate(con, "set search_path to common_tables,public;")
dbGetQuery(con, "show search_path;")


#Delete
returnDelete2<-dbSendUpdate(con,"delete from common_tables.eventsnamedparts_nlp where notification_number in (
            					select notification_number
						from common_tables.nlp_m_sap_notification_processed
						where  ((notification_datetime >= dateadd(day, -15,current_date) and notification_datetime <= current_date)
						or (notification_changed_date >= dateadd(day, -15,current_date) and notification_changed_date <= current_date))
 						and codingdescr='Event Report'
						and operation is not NULL and asset is not NULL and asset in ('Pampa Norte','Escondida')
						)")
returnCommit2<-dbCommit(con)

### WRITE FLAG FILE ###
FLAG_Spanish_All5<-format(Sys.time(), "%Y-%m-%d %H:%M:%S")

setwd(paste(baseWD,"\\Output",sep=""))
write.table(FLAG_Spanish_All5, "FLAG_Spanish_All5.txt",
            na = "",
            row.names = FALSE,
            col.names = FALSE,
            sep = "\t")


dbSendUpdate(con,"update common_tables.t_error_log
			set
				run_status='SUCCESS',
				run_status_message='',
				run_end_datetime=(select SYSDATE)
			 where code_description='Spanish Combine Text'
			 and run_start_datetime=(select max(run_start_datetime) from common_tables.t_error_log where code_description='Spanish Combine Text')")
dbCommit(con)

#######################################################################
####0206-Remove existing metadata and last output file
#######################################################################
setwd(paste(baseWD,"\\Interim",sep=""))
list.files(getwd())
file.remove(list.files(getwd()))

